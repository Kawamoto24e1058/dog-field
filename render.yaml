services:
  - type: web
    name: dog-field
    env: node
    plan: starter
    buildCommand: npm install
    startCommand: node server/index.js
    envVars:
      - key: PORT
        value: 8080
      - key: NODE_ENV
        value: production
    autoDeploy: true
    healthCheckPath: /health
